# Aegis ATLAS 사용자 매뉴얼

이 문서는 Aegis 회계담당자, 회장, 검토자와 관리자가 ATLAS를 사용해 장부와 증빙을
정리하고 동아리연합회 제출 패키지 및 월간 공개 자료를 만드는 절차를 설명한다.

## 1. 계좌 구성

ATLAS는 두 계좌를 완전히 분리해 관리한다.

| 구분 | ATLAS 표시 | 용도 | 제출용 계좌 자료 |
| --- | --- | --- | --- |
| 운영계좌 | 동아리운영계좌(우리은행) | 동아리 운영비 지출, 지원금·회비 이체 수령 등 | 계좌 전체내역 캡처 |
| 회비계좌 | 회비입금계좌(IBK기업은행) | 회원 회비 입금 검증 및 환불 | 계좌 전체내역 캡처 |

Google Sheets 장부, 증빙, 계좌 캡처와 스냅샷은 모두 계좌별로 저장된다. 운영계좌와
회비계좌에서 같은 장부 번호를 사용하더라도 서로 섞이지 않는다.

## 2. 로그인과 권한

1. ATLAS 루트 주소에 접속한다.
2. `Google 계정으로 로그인`을 누른다.
3. 화이트리스트에 등록된 Google 계정으로 로그인한다.

권한은 다음과 같다.

| 역할 | 주요 권한 |
| --- | --- |
| 관리자 | 전체 기능, 사용자 화이트리스트 및 역할 관리 |
| 회장 | 패키지 생성·검토·승인, 월간 공개와 Discord 전송 |
| 총무 | 장부·증빙·계좌 캡처 연결, 패키지 생성 |
| 검토자 | 장부와 결과 조회, 검토 및 승인 흐름 참여 |

로그인할 수 없는 경우 관리자에게 `설정` 페이지의 화이트리스트 등록 여부와 역할을 확인해
달라고 요청한다. 로그인하지 않은 상태에서 내부 주소로 접근하면 루트 로그인 화면으로 이동한다.

## 3. 사전 준비

작업 전에 다음 자료를 준비한다.

- 동아리운영계좌(우리은행) Google Sheets 회계장부
- 회비입금계좌(IBK기업은행) Google Sheets 회계장부
- 영수증과 소명자료
- 두 계좌의 계좌 전체내역 캡처 파일
- 동아리명, 회계담당자명, 회장명, 검토자명

Google Sheets 장부의 데이터 범위는 `B:I`이며 열 순서는 다음과 같아야 한다.

| 열 | 값 |
| --- | --- |
| B | No |
| C | 날짜 |
| D | 내용 |
| E | 수입 |
| F | 지출 |
| G | 잔액 |
| H | 처리방식 |
| I | 상세정보 |

처리방식에는 `계좌이체`, `카드결제`, `카드결제취소`, `이자입금`, `이자입금취소` 등을
입력한다. `카드결제`와 `카드결제취소` 거래에는 영수증 또는 소명자료가 반드시 필요하다.

## 4. 계좌 전체내역 캡처 준비

동연 제출 기간에 해당하는 전체 계좌내역을 각 은행 앱 또는 웹에서 캡처한다. ATLAS는 은행에서
내려받은 Excel이나 PDF 거래내역 파일을 입력 자료로 사용하지 않는다.

캡처 준비 시 다음을 확인한다.

- 동아리운영계좌(우리은행)와 회비입금계좌(IBK기업은행)를 각각 준비한다.
- 계좌 구분, 조회 기간과 거래 목록을 알아볼 수 있도록 캡처한다.
- 한 화면에 전체 기간이 들어오지 않으면 여러 이미지 또는 여러 페이지 PDF로 준비한다.
- 파일명에 `운영계좌`, `우리은행`, `회비입금`, `기업은행` 등의 계좌 구분을 넣으면 자동 분류에 도움이 된다.
- 자동 분류가 어려운 이름이라면 업로드 전에 화면의 계좌 분류를 직접 선택한다.
- 계좌번호와 개인정보가 포함될 수 있으므로 파일을 GitHub에 커밋하지 않는다.

## 5. 장부/증빙 페이지

### 5.1 기간 지정

1. 시작일과 종료일을 선택한다.
2. 시작일 기본값은 당해 연도 1월 1일이다.
3. 동연 패키지에서는 시작일이 당해 연도 1월 1일로 고정된다.

### 5.2 Google 장부 연결

1. `Google 자료 연결`에서 Aegis Google 계정을 연결한다.
2. 운영계좌 장부 URL 또는 Spreadsheet ID를 입력한다.
3. 회비계좌 장부 URL 또는 Spreadsheet ID를 입력한다.
4. 범위는 기본 `B:I`를 사용한다. 특정 시트라면 `시트명!B:I`로 입력한다.
5. `운영 장부 연결`, `회비 장부 연결`을 각각 누른다.

장부를 다시 연결하면 새 스냅샷이 생성된다. 이전 장부 스냅샷은 덮어쓰지 않는다.

### 5.3 영수증·소명·계좌 캡처 업로드

파일명 앞 표식으로 계좌와 장부 번호를 자동 지정할 수 있다.

| 표식 | 대상 | 예시 |
| --- | --- | --- |
| `#장부ID#` | 동아리운영계좌(우리은행) | `#24# 2026. 3. 3. 프린터 결제.pdf` |
| `*장부ID*` | 회비입금계좌(IBK기업은행) | `*6* 2026. 3. 1. 회비 환불 소명.png` |

1. 가능하면 파일명에 위 표식을 붙인다.
2. 여러 파일을 `영수증·소명·캡처`에서 한 번에 선택한다.
3. 파일명이 자료 종류를 설명하지 않으면 `자료 종류`에서 영수증, 소명자료 또는 계좌 전체내역 캡처를 직접 선택한다.
4. 영수증과 소명자료에 파일명 표식이 없으면 화면에서 계좌와 장부 ID를 직접 지정한다.
5. 계좌 전체내역 캡처는 장부 ID 없이 업로드하고 계좌만 자동 또는 수동으로 분류한다.
6. 업로드 후 거래 목록의 증빙 개수를 확인한다.
7. 돋보기 버튼으로 연결된 증빙을 미리 본다.

PDF, JPEG, JPG, PNG, HEIC, HEIF는 문서에 삽입할 수 있다. DOCX처럼 직접 렌더링하지
못하는 형식도 원본자료에서는 누락되지 않는다. macOS에서 생성된 NFD 파일명은 ATLAS가
NFC로 정규화한다.

### 5.4 거래 목록 확인

`전체 계좌`, `운영계좌`, `회비계좌` 필터를 사용해 장부 거래를 확인한다. 날짜, 내용,
수입·지출, 잔액, 처리방식, 상세정보와 증빙 연결 수를 확인한다. 이 화면에서는 장부를
직접 수정하지 않으며, 원본 Google Sheets에서 수정한 뒤 장부를 다시 연결한다.

### 5.5 장부와 은행 거래내역 수동 대조

ATLAS는 은행 거래내역과 장부의 금액·날짜를 자동 대조하지 않는다. 여러 은행 거래를 장부 한
건으로 일괄 기입하거나, 반대로 한 거래를 여러 장부 항목으로 나누는 경우가 있어 단순한 1:1
금액 비교로는 정상 거래도 오류로 판정될 수 있기 때문이다. 카드 승인일과 실제 출금일 차이,
수수료, 환불, 결제 취소와 두 계좌 사이의 이체도 자동 판정에 영향을 준다.

동연 패키지를 만들기 전에 회계담당자가 두 계좌를 각각 다음 순서로 대조한다.

1. 장부와 은행 앱의 조회 기간을 같은 시작일과 종료일로 맞춘다.
2. 동아리운영계좌(우리은행)와 회비입금계좌(IBK기업은행)를 섞지 않고 따로 확인한다.
3. 은행 거래 한 건마다 대응하는 장부 ID를 찾는다.
4. 여러 은행 거래를 장부 한 건으로 합쳤다면 관련 은행 거래 금액의 합계와 장부 금액을 비교한다.
5. 은행 거래 한 건을 장부 여러 건으로 나눴다면 관련 장부 금액의 합계와 은행 거래 금액을 비교한다.
6. 날짜가 다르면 카드 승인일·출금일, 환불 처리일, 휴일 이체 여부를 확인하고 장부 `상세정보`에 사유를 남긴다.
7. 일치하지 않는 항목은 아래 불일치 유형별 조치에 따라 원본 Google Sheets를 수정하거나 소명자료를 준비한다.
8. 장부를 수정했다면 ATLAS에서 해당 Google 장부를 다시 연결해 새 스냅샷을 생성한다.
9. 수정 후 거래 목록과 동연 패키지의 검증 결과를 다시 확인한다.

별도 대조표를 만들 때는 다음 열을 사용하는 것을 권장한다.

| 계좌 | 은행 거래일 | 은행 거래내용 | 은행 금액 | 대응 장부 ID | 장부 합계 | 차이 | 확인 결과·비고 |
| --- | --- | --- | ---: | --- | ---: | ---: | --- |
| 운영/회비 | YYYY-MM-DD | 입금자·결제처 | 입금은 `+`, 출금은 `-` | 여러 건이면 쉼표로 구분 | 원 | 원 | 일치/수정/소명 |

불일치 유형별 조치는 다음과 같다.

| 유형 | 확인 사항 | 조치 |
| --- | --- | --- |
| 은행에는 있고 장부에는 없음 | 이자, 수수료, 환불, 결제 취소, 계좌 간 이체, 누락 거래 | 누락이면 장부에 추가하고, 이미 다른 장부 항목에 합산됐다면 대응 장부 ID와 산식을 대조표에 기록 |
| 장부에는 있고 은행에는 없음 | 현금 처리, 승인 후 미출금, 잘못된 계좌 선택, 중복 기입 | 실제 처리 여부를 확인해 장부를 수정하거나 필요한 소명자료 준비 |
| 금액이 다름 | 일괄 기입, 분할 기입, 수수료 포함, 일부 환불 | 관련 거래를 묶어 합계를 재계산하고 차액 사유를 상세정보 또는 소명자료에 기록 |
| 날짜가 다름 | 카드 승인일과 출금일 차이, 예약 이체, 휴일, 환불 처리 시차 | 동일 거래임을 확인한 뒤 실제 회계 기준일을 유지하고 상세정보에 날짜 차이 사유 기록 |
| 두 계좌에 걸친 이체 | 회비입금계좌에서 운영계좌로 월 단위 일괄이체 | 양쪽 장부에서 대응 거래를 확인하고 중복 수입·지출로 계산되지 않았는지 검토 |

수동 대조표 자체는 ATLAS 필수 업로드 자료가 아니다. 다만 금액이나 날짜 차이를 제3자가 바로
이해하기 어려운 경우에는 해당 장부 ID에 소명자료를 연결해 제출 패키지에 포함한다.

## 6. 동연 패키지 생성

동연 패키지는 ATLAS의 핵심 기능이다.

### 6.1 준비 상태 확인

두 계좌 모두 다음 자료가 있어야 한다.

- Google Sheets 장부 스냅샷
- 계좌 전체내역 캡처
- 카드결제·카드결제취소 거래의 필수 영수증 또는 소명자료
- 두 계좌의 장부와 실제 은행 거래내역 수동 대조 완료

ATLAS 화면은 장부·캡처·증빙 등록 상태만 확인한다. 필요한 시스템 자료가 없으면 해당 계좌
상태가 `REQUIRED` 또는 `PREPARING`으로 표시된다. 수동 대조 완료 여부는 자동 판정하지
않으므로 회계담당자와 검토자가 별도 체크리스트로 확인한다.

### 6.2 패키지 자료 추가

`영수증·소명·계좌 전체내역 캡처` 영역에서 추가 자료를 일괄 등록할 수 있다. 파일명에
`#장부ID#` 또는 `*장부ID*`가 있으면 자동으로 계좌별 장부 거래에 연결된다.

### 6.3 제출 정보 입력 및 생성

1. 동아리명과 회계 기간을 확인한다.
2. 회계담당자, 회장, 검토자 이름을 입력한다.
3. `통합 패키지 생성`을 누른다.
4. 작업 상태가 완료될 때까지 기다린다.
5. 검증 결과의 오류와 경고 수를 확인한다.
6. `결재 요청`, `승인`, `ZIP 다운로드` 순서로 진행한다.

`Discord 결재 요청`을 누르면 `.env`에 설정한 Webhook 채널로 임베드 알림이
전송된다. 알림에는 지정한 사용자·역할 멘션, 회계 기간, 패키지 버전,
검증 결과와 ATLAS 결재 링크가 포함된다. 링크로 접근한 뒤 Google 로그인을
완료하면 해당 패키지 결재 화면으로 돌아간다. 관리자·회장·검토자는 결재 의견을
입력한 뒤 승인하거나 반려할 수 있고, 처리 결과도 같은 Discord 채널로 전송된다.

Webhook은 Discord 채널의 `채널 편집 > 연동 > Webhook`에서 생성한다. 개발자
모드에서 사용자나 역할을 우클릭해 `ID 복사`한 값을 다음처럼 설정한다.

```ini
ATLAS_DISCORD_APPROVAL_WEBHOOK_URL=https://discord.com/api/webhooks/...
ATLAS_DISCORD_APPROVAL_USER_IDS=123456789012345678
ATLAS_DISCORD_APPROVAL_ROLE_IDS=234567890123456789,345678901234567890
```

Webhook URL이 들어 있는 `.env`는 Git에 커밋하지 않는다. Webhook은 생성한 해당 채널에만
전송하므로 별도의 채널 ID는 필요하지 않다.

`검증_리포트.html`에는 장부 잔액, 기간, 거래 번호·날짜 순서, 중복 거래, 필수 카드 증빙과
증빙 접근성 검증 결과가 기록된다. 특정 거래에서 발생한 오류와 경고에는 해당 `장부 ID`가
표시되며, 여러 거래에 걸친 증빙 중복 오류에는 관련 장부 ID가 모두 표시된다.

검증 리포트의 `PASS`는 장부 내부 계산과 증빙 규칙을 통과했다는 의미이며, 실제 은행 거래와의
대조까지 자동 완료됐다는 의미는 아니다. 패키지 승인 전에는 5.5의 수동 대조를 별도로 완료한다.

### 6.4 ZIP 결과물

통합 패키지에는 다음 파일이 포함된다.

- `동아리 계좌 전체내역.docx`: 두 계좌의 업로드된 계좌 전체내역 캡처
- `동아리 수입지출관리대장.xlsx`: 한 파일 안에서 두 계좌를 시트별로 분리
- `영수증 및 소명자료 - 동아리운영계좌(우리은행).docx`
- `영수증 및 소명자료 - 회비입금계좌(IBK기업은행).docx`
- `검증_리포트.html`: 장부와 증빙 검증 결과
- `manifest.json`: 스냅샷, 파일 목록과 SHA-256 무결성 정보
- `증빙 원본/`: 계좌별 영수증과 소명 원본
- `원본자료/`: 업로드한 Aegis 장부 원본

수입지출관리대장은 거래 수에 따라 동아리연합회 공식 40칸, 80칸, 120칸 원본 양식을
선택하고 기존 서식에 값만 입력한다. 거래가 120건을 초과하면 패키지 생성을 중단한다.

## 7. 월간 공개

1. 공개할 연월을 선택한다.
2. 링크 유효기간을 설정한다.
3. 월간 공개 자료를 생성한다.
4. 공개 페이지에서 선택한 달의 운영계좌 거래만 표시되는지 확인한다.
5. Discord 메시지를 미리 본다.
6. 회계담당자 또는 회장이 승인한 뒤 전송한다.
7. 더 이상 공개하지 않을 링크는 공개 링크 옆 휴지통 버튼으로 삭제한다.

Discord에는 핵심 월간 요약과 공개 링크만 전송한다. 승인 전에는 실제 Webhook 전송이
발생하지 않는다. 공개 링크가 만료되거나 삭제되면 외부에서 더 이상 접근할 수 없고 월간 공개
화면의 현재 링크 표시에서도 자동으로 사라진다.

## 8. 스냅샷 관리

Google 장부 연결과 증빙·계좌 캡처 연결은 새 스냅샷을 자동 생성한다.

- `현재 상태 스냅샷 생성`: 최신 상태를 수동 체크포인트로 복제한다.
- `복원`: 선택한 과거 스냅샷을 새 최신 버전으로 복제한다.
- 기존 이력은 삭제하거나 덮어쓰지 않는다.
- 운영계좌와 회비계좌의 스냅샷 이력은 별도로 관리된다.

중요 작업 직전과 동연 패키지 생성 직후에는 수동 체크포인트를 남기는 것을 권장한다.

## 9. 설정

관리자는 `설정` 페이지에서 로그인 화이트리스트를 관리한다.

1. 사용자 Google 이메일을 입력한다.
2. 관리자, 회장, 총무, 검토자 중 역할을 선택한다.
3. 사용자를 추가한다.
4. 권한이 변경되거나 회수된 사용자는 목록에서 역할을 변경하거나 삭제한다.

`.env`의 `ATLAS_ADMIN_EMAIL`은 최초 관리자 계정이다. Google OAuth Client ID와 Secret,
기본 장부 URL, 공개용 Spreadsheet URL 같은 운영 설정은 `.env`에서 관리한다.

## 10. 자주 발생하는 문제

### Google 로그인 후 다시 로그인 화면이 나오는 경우

- 해당 이메일이 화이트리스트에 등록되어 있는지 확인한다.
- 브라우저의 세션 저장소가 차단되지 않았는지 확인한다.
- 운영 도메인이 Google OAuth 승인된 리디렉션 URI에 등록되어 있는지 확인한다.

### Google Sheets 연결이 실패하는 경우

- Aegis Google 계정 연결 상태를 확인한다.
- Google Sheets API와 Google Drive API가 활성화되어 있는지 확인한다.
- 장부 URL과 `B:I` 또는 `시트명!B:I` 범위를 확인한다.
- Cloudflare Tunnel의 백엔드 호스트와 컨테이너 상태를 확인한다.

### 계좌 전체내역 캡처가 패키지에 없는 경우

- 업로드 파일명이 `거래내역`, `계좌전체` 또는 `계좌캡처`를 포함하는지 확인한다.
- 파일명 자동 분류가 어려우면 업로드 전에 계좌 분류를 직접 선택한다.
- 동연 패키지 준비 상태에서 두 계좌의 `계좌 전체내역 캡처`가 각각 등록됐는지 확인한다.
- 은행 Excel/PDF 거래내역은 장부 파일 가져오기에서 지원하지 않으므로 이미지 또는 PDF 캡처로 등록한다.

### 패키지 검증에서 카드 증빙 오류가 발생하는 경우

- `카드결제`, `카드결제취소` 거래에 영수증 또는 소명자료가 연결되었는지 확인한다.
- 운영계좌는 `#장부ID#`, 회비계좌는 `*장부ID*` 표식을 확인한다.
- 같은 증빙이 여러 거래에 중복 연결되지 않았는지 확인한다.

### 문서 생성이 오래 걸리는 경우

PDF/HEIC 증빙과 계좌 캡처가 많으면 문서 변환에 시간이 걸릴 수 있다. 화면을 닫지 말고 현재
작업 상태를 확인한다. 30분 이상 완료되지 않으면 백엔드 로그와 저장 공간을 확인한다.

## 11. 운영 보안 및 백업

- `.env`, `backend/storage`, 계좌 캡처와 증빙 원본을 GitHub에 올리지 않는다.
- 운영 백업에는 `backend/storage` 전체를 포함한다.
- `ATLAS_SECRET_KEY`를 바꾸면 기존 OAuth 토큰과 Discord Webhook을 복호화할 수 없다.
- 월간 공개용 Google Spreadsheet에는 내부 문서를 함께 보관하지 않는다.
- Discord 전송은 대상 채널과 공개 링크 내용을 검토한 뒤 승인한다.
- 동연 제출 전에는 생성된 ZIP을 직접 열어 네 개의 핵심 문서와 원본자료를 확인한다.
